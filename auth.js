/* ============================================================
   STARK QUANTUM LAB · 검사지 접근 비밀번호 설정 파일
   ------------------------------------------------------------
   이 파일에는 비밀번호 원문이 아니라 SHA-256 해시만 저장합니다.
   (소스를 열어 봐도 비밀번호 자체는 보이지 않습니다)

   ■ 현재 비밀번호: 789456$$   ← 배포 전에 반드시 변경하세요!

   ■ 비밀번호 바꾸는 방법
   1. 크롬에서 F12 → Console 탭을 열고 아래 한 줄을 붙여넣은 뒤
      '새비밀번호' 부분만 바꿔 실행:

      crypto.subtle.digest('SHA-256', new TextEncoder().encode('새비밀번호'))
        .then(b => console.log([...new Uint8Array(b)]
        .map(x => x.toString(16).padStart(2,'0')).join('')));

   2. 출력된 64자리 문자열을 아래 PASS_HASH 값에 붙여넣고 저장.
   3. GitHub에 이 파일만 다시 업로드하면 즉시 적용됩니다.

   ■ 알아 두실 것
   GitHub Pages는 서버가 없는 정적 호스팅이라 이 방식은
   "일반 방문자를 막는 잠금장치"이지 완전한 보안은 아닙니다.
   개발자 도구를 다룰 줄 아는 사람은 우회할 수 있습니다.
   문항 유출이 정말 중요해지면 서버 인증(예: Cloudflare Access,
   Netlify 비밀번호 보호, 간단한 백엔드)으로 옮기는 것을 권합니다.
   ============================================================ */

window.STARK_AUTH = {
  PASS_HASH: "99f5c30fea14bf97c0f61dcc2f471edc646560899b44b7871667c9c0fe59a52a",
  SESSION_KEY: "sql_test_unlocked",   // 브라우저 탭이 열려 있는 동안만 유지
  MAX_TRIES: 5                        // 연속 오답 허용 횟수
};
